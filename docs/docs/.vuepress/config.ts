import { defineUserConfig } from 'vuepress'

export default defineUserConfig({
  lang: 'en-US',
  title: 'Documentation Based on Components',
  description: 'A UI Definition documentation based on components, compositions and pattern desings',
})
